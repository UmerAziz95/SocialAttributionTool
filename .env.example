# Example local configuration. Copy to `.env` and update values before running the app.
ENV=dev
DEBUG=true
INIT_DB_ON_STARTUP=true
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/social_attribution
JWT_SECRET=change_me
CORS_ORIGINS=["http://localhost:3000"]
